---

LOCALE_LANG: en_US.UTF-8
TIMEZONE: UTC

DB_USER: adllrs
DB_PASSWORD: adllrs
DB_NAME: lrs

RABBITMQ_USER: adllrs
RABBITMQ_PASSWORD: adllrs
RABBITMQ_VHOST: lrs

# REMOTE_USER: has sudo powers and used to install
REMOTE_USER: root

# APP_USER: app-level user, typically no sudo powers (used by lrs_app)
APP_USER: lrs
USER_HOME: "/home/{{APP_USER}}/"
PROJECT_ROOT: "{{USER_HOME}}{{PROJECT_NAME}}/"

# Django App
VIRTUALENV_NAME: lrs
WORKON_HOME: "{{USER_HOME}}.virtualenvs/"
VENV_PATH: "{{WORKON_HOME}}{{VIRTUALENV_NAME}}/"
VIRTUALENVWRAPPER_LOCATION: /usr/local/bin/virtualenvwrapper.sh
SITE_PACKAGES_DIR: "{{VENV_PATH}}local/lib/python2.7/site-packages/"

DJANGO_SECRET_KEY: "Some long random string with numb3rs and $ymbol$"

# LRS_LOG_DIR_ROOT: "{{PROJECT_ROOT}}logs/"
# LRS_MEDIA_ROOT: "{{PROJECT_ROOT}}media/"

# Git-related
project_repo: git@github.com:kgrandis/ADL_LRS.git
project_branch: develop
PROJECT_NAME: ADL_LRS

# Default (overridden in Vagrantfile)
is_vagrant: false

# Ansible convenience
is_ubuntu: "{{ansible_distribution == 'Ubuntu'}}"
